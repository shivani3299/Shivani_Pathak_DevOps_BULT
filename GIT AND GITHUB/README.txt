This is the assignment of iTransform git and github

In this folder I have created a simple web page using HTML,CSS and JavaScrip.
In addition to that i have given a button to change a theme from dark to light theme.

Created this whole folder in Visual Studio and committed changes.


Git commands are given as follows:=

git init
git status 
git log
git add . or git add file_name
git commit -m "commi_message"
git commit -am "commit_message"
git remote add origin github_link
git push -u origin
git reflog
